conda create -n dinosparse python=3.11.15
conda activate dinosparse

# 看网络吧，随便试试
pip install torch==2.7.1 -f https://mirrors.aliyun.com/pytorch-wheels/cu128
pip install torch==2.7.1 torchaudio==2.7.1 -f https://mirrors.aliyun.com/pytorch-wheels/cu128
pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 -f https://mirrors.aliyun.com/pytorch-wheels/cu128

pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 --index-url https://download.pytorch.org/whl/cu128

# pycocotools
cd D:\MIaMIA\other-pkgs\dinosparse
pip install pycocotools-2.0.11-cp311-cp311-win_amd64.whl

# fvcore
cd D:\MIaMIA\other-pkgs\dinosparse\fvcore-main
python setup.py build --force develop

# detectron2
cd D:\MIaMIA\other-pkgs\dinosparse\detectron2-main
pip install -e . --no-build-isolation

# 在python通过import验证，不报错即可
目前最新版的0.6无法编译，貌似后面官方修改过一些东西，虽然都叫0.6
--

pip install opencv-python --dry-run
pip install opencv-python
pip install scikit-learn --dry-run
pip install scikit-learn

pip install transformers safetensors --dry-run
pip install transformers safetensors

# 处理.mat
pip install h5py --dry-run
pip install h5py
--

待执行：


pip uninstall pywin32
pip install pywin32
pip uninstall portalocker
pip install portalocker

# analyze_model
pip install protobuf==3.20.3 --dry-run
pip install protobuf==3.20.3






--


可能有用的复制粘贴
conda config --show
conda config --add channels xxx
conda config --remove channels xxx

pip config list
pip config get global.index-url
pip config set global.index-url https://mirrors.aliyun.com/pytorch-wheels/cu128/
pip config unset global.index-url

cd D:\MIaMIA\other-pkgs\dinosparse
pip install torch-2.7.1+cu128-cp311-cp311-win_amd64.whl --dry-run


选用：
pip cache purge

conda activate base
conda env remove -n DEViT

双关，删env文件

conda create -n DEViT python=3.10
conda activate DEViT
cd D:\MIaMIA\PycharmProjects\20240225_prac_DEViT
检查pip list

conda install pytorch==2.2.0 torchvision==0.17.0 torchaudio==2.2.0 pytorch-cuda=11.8 -c pytorch -c nvidia
pip install -e .
pip install -r requirements1.txt

conda clean --all